{{- if .Values.postgresql.internal }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyspace.fullname" . }}-postgresql
  labels:
    {{- include "hyspace.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/name: hyspace
    app.kubernetes.io/instance: {{ include "hyspace.fullname" . }}
    app.kubernetes.io/component: postgresql
{{- end }}
